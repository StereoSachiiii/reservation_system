// CORS config removed — redundant because frontend and API are served
// from the same origin via the Nginx reverse proxy.
// If you ever need cross-origin access again (e.g., separate frontend domain),
// re-add a CorsConfigurationSource bean here.
package com.bookfair.config;

